{
  "buildCommand": "cd frontend && npm run build && rm -rf ../dist && mv dist ../dist",
  "outputDirectory": "dist",
  "installCommand": "npm install && cd frontend && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}